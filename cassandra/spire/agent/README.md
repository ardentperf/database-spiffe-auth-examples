# SPIRE agent

This is a basic configuration for running a SPIRE agent without verifying the SPIRE server's identity. The SPIRE
agent attests with a static JoinToken and can attest unix processes or Docker containers.